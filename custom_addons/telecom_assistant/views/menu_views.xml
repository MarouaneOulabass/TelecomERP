<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!--
        L'assistant n'a PAS de menu racine. L'accès se fait uniquement
        via le widget popup flottant (FAB en bas à droite).

        Seule exception : l'historique des conversations, accessible
        depuis le widget lui-même via le lien "Historique".
    -->

    <!-- Action historique (appelée depuis le widget, pas depuis un menu principal) -->
    <!-- L'action existe dans telecom_assistant_views.xml : action_assistant_conversation -->
</odoo>
